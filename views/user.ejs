<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>All users</title>
</head>
<body>
    <h1>ALL users</h1>
    <h2> Click user to see Data </h2>
    <ul>
        <% users.forEach(user => { %>
            <li>
                <a href="/profile/<%= user.username %>">
                    <%= user.name %>
                </a>
            </li>
        <% }) %>
    </ul>
</body>
</html>
